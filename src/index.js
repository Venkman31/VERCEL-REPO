import React from 'react';
import ReactDOM from 'react-dom/client';
import BudgetForecastingTool from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BudgetForecastingTool />
  </React.StrictMode>
);
